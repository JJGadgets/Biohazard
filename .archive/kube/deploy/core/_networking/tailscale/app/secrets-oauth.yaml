---
apiVersion: v1
kind: Secret
metadata:
  name: operator-oauth
  namespace: tailscale
type: Opaque
stringData:
  client_id: "${SECRET_TAILSCALE_OAUTH_CLIENT_ID}"
  client_secret: "${SECRET_TAILSCALE_OAUTH_CLIENT_SECRET}"