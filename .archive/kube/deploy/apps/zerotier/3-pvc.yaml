---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zerotier-one
  namespace: zerotier
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: block
  resources:
    requests:
      storage: 1Gi
