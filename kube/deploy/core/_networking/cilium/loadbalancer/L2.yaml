---
apiVersion: cilium.io/v2alpha1
kind: CiliumL2AnnouncementPolicy
metadata:
  name: "l2"
spec:
  externalIPs: true
  nodeSelector:
    matchLabels:
      kubernetes.io/os: "linux"
  serviceSelector:
    matchExpressions:
      - key: "io.cilium/l2"
        operator: Exists
