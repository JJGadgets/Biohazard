---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: vector-config
    namespace: vector
    files:
      - fortigate.yaml
generatorOptions:
  disableNameSuffixHash: true
