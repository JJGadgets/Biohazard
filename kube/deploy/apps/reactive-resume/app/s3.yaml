---
apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: "reactive-resume-media-s3"
  namespace: "reactive-resume"
spec:
  storageClassName: "rgw-${CLUSTER_NAME}"
  bucketName: "reactive-resume-media"
