---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kah
  namespace: kah
spec:
  secretName: kah-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - '*.${DNS_KAH}'