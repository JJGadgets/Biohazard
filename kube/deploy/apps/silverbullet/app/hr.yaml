---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/app-template-4.6.2/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app silverbullet
  namespace: *app
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: *app
  values:
    controllers:
      app:
        type: deployment
        replicas: 1
        pod:
          labels:
            ingress.home.arpa/envoy-internal: allow
        containers:
          app:
            image: &img
              repository: ghcr.io/silverbulletmd/silverbullet
              tag: 2.5.0@sha256:aa414594607b29ad20fbb7b473dcc46f4e345511873b248273a970cf9e58887e
            env: &env
              TZ: "${CONFIG_TZ}"
              SB_PORT: &http 8080
              SB_NAME: JJGadgets Notes
              SB_DESCRIPTION: Silverbullet note taking app, hosted by and for JJGadgets.
            # envFrom: &envFrom
            #   - secretRef:
            #       name: silverbullet-secrets
            securityContext: &sc
              readOnlyRootFilesystem: true
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
            resources:
              requests:
                cpu: "10m"
              limits:
                cpu: "1"
                memory: "100Mi"
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
    service:
      app:
        controller: app
        ports:
          http:
            port: 80
            targetPort: *http
            protocol: HTTP
            appProtocol: http
    route:
      app: &route
        hostnames: ["${APP_DNS_SILVERBULLET:=silverbullet}"]
        parentRefs:
          - name: envoy-internal
            namespace: ingress
            sectionName: internal
      unauth:
        <<: *route
        rules:
          - matches:
              - path:
                  type: Exact
                  value: "/service_worker.js"
              - path:
                  type: PathPrefix
                  value: "/.client/*"
            backendRefs:
              - identifier: app
                port: http
    persistence:
      data:
        existingClaim: silverbullet-data
        globalMounts:
          - subPath: data
            path: /space
    defaultPodOptions:
      automountServiceAccountToken: false
      enableServiceLinks: false
      dnsConfig:
        options:
          - name: ndots
            value: "1"
      hostUsers: false
      securityContext:
        runAsNonRoot: true
        runAsUser: &uid 1000
        runAsGroup: *uid
        fsGroup: *uid
        fsGroupChangePolicy: Always
        seccompProfile: { type: "RuntimeDefault" }
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: "fuckoff.home.arpa/{{ .Release.Name }}"
                    operator: DoesNotExist
