---
.substitutions: &subs !include
  file: "./.metadata.yaml"
  vars:
    area: &area "JJ's Room"
    name: &name "AC - CN105"
substitutions:
  <<: *subs
  area: *area
  name: *name
  board: "lolin_s3_mini"
packages:
  - !include ./.mitsubishi-cn105.yaml
  - !include ./.bluetooth-proxy.yaml
