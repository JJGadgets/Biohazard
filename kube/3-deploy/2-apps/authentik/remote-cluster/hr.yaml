---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: remote-cluster
  namespace: authentik
spec:
  chart:
    spec:
      chart: authentik-remote-cluster
      version: 1.1.0
      sourceRef:
        name: authentik
        kind: HelmRepository
        namespace: flux-system
