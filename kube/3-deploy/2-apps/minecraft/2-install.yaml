---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: echo-server
  namespace: default
spec:
  chart:
    spec:
      chart: minecraft
      version: 4.5.0
      sourceRef:
        name: minecraft
  values:
    replicaCount: 1
    strategyType: Recreate
    minecraftServer:
      eula: "TRUE"
      version: "1.19.3"
      type: "FABRIC"
