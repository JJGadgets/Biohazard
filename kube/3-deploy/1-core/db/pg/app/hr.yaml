---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cloudnative-pg
  namespace: cnpg
spec:
  chart:
    spec:
      chart: cloudnative-pg
      version: 0.18.0
      sourceRef:
        name: cloudnative-pg
        kind: HelmRepository
        namespace: flux-system
  values:
    image:
      tag: "1.20.0"
    crds:
      create: true
