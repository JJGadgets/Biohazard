---
name: Purge README Image Cache
on:
  workflow_dispatch:
  schedule:
    - cron: 0 * * * * # hourly
  push:
    branches: ["main"]
    paths:
      - "README.md"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Purge
        uses: kevincobain2000/action-camo-purge@0b70f4d64c9828aace7bbbf1931a646957734ef3 # v1
